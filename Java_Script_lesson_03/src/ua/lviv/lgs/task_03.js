function find(arr) {
	let indexOff = arr.indexOf('value');
	console.log('Object "value" is find');
	console.log('The objeckt index = ' + indexOff);
}


let arr = [
	'main',
	'car',
	'value',
	'book'
];

find(arr);






















/*
Створіть функцію find(arr, value), 
яка шукає в масиві arr значення value і повертає його номер, 
якщо знайдено, або -1, якщо не знайдено.
*/